import React, { useState } from 'react';\nimport Payment from './pages/guest/Payment';\n\n// Sample usage of the Payment component\nconst PaymentIntegration = () => {\n  const [currentPage, setCurrentPage] = useState('payment');\n  \n  // Sample booking data - this would come from your booking process\n  const sampleBookingData = {\n    bookingId: 'BK001234',\n    vehicleNumber: 'MH12AB1234',\n    vehicleType: 'Car',\n    slotNumber: 'A-15',\n    ownerName: 'John Doe',\n    mobile: '+91 9876543210',\n    entryTime: '10:30',\n    exitTime: '14:45'\n  };\n\n  const handleNavigation = (page) => {\n    setCurrentPage(page);\n    console.log(`Navigating to: ${page}`);\n  };\n\n  return (\n    <div>\n      {currentPage === 'payment' && (\n        <Payment \n          bookingData={sampleBookingData}\n          onNavigate={handleNavigation}\n        />\n      )}\n      \n      {/* Other pages would be rendered here based on currentPage */}\n      {currentPage === 'home' && (\n        <div style={{ padding: '24px', textAlign: 'center' }}>\n          <h2>Home Page</h2>\n          <button onClick={() => setCurrentPage('payment')}>Go to Payment</button>\n        </div>\n      )}\n      \n      {currentPage === 'book-slot' && (\n        <div style={{ padding: '24px', textAlign: 'center' }}>\n          <h2>Book Slot Page</h2>\n          <button onClick={() => setCurrentPage('payment')}>Proceed to Payment</button>\n        </div>\n      )}\n      \n      {currentPage === 'exit-checkout' && (\n        <div style={{ padding: '24px', textAlign: 'center' }}>\n          <h2>Exit Checkout Page</h2>\n          <button onClick={() => setCurrentPage('home')}>Back to Home</button>\n        </div>\n      )}\n    </div>\n  );\n};\n\nexport default PaymentIntegration;